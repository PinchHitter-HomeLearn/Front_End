<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pinch_hitter_homelearn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".main.FindpersonFragment"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".main.HistoryFragment"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".main.ChatAndAlarmFragment"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".main.PositionFragment"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".main.HomeFragment"
            tools:ignore="Instantiatable"></activity>
        <activity
            android:name=".main.FragmentTab"
            tools:ignore="Instantiatable" />
        <activity android:name=".auth.SignUpActivity2" />
        <activity android:name=".auth.Find_PW_Activity" />
        <activity android:name=".auth.SignUpActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".splashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.MainActivity">

        </activity>
    </application>

</manifest>